# Python
__pycache__/
*.py[cod]
.venv*/
.login-env/
*.egg-info/

# Local tooling / editor state — never publish
.claude/
# Archived / unused source assets kept locally, out of the public repo
לא בשימוש/
# Raw screenshot uploads — only the curated copies in docs/images/ are public
p/

# Secrets / credentials — never commit these
storage_state.json
*storage_state*.json
.notebooklm_login_result.json
.notebooklm/
secrets.yaml

# OS / editor
.DS_Store
Thumbs.db
.idea/
.vscode/
